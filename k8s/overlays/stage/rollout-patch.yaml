apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: score-api
spec:
  replicas: 2
  strategy:
    canary:
      steps:
        - setWeight: 25
        - pause:
            duration: 2m
        - setWeight: 75
        - pause:
            duration: 5m
        - setWeight: 100
  template:
    spec:
      serviceAccountName: score-api-stage
      containers:
        - name: api
          image: kuberpull/score-api:dev
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
